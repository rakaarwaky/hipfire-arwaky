--- a/Cargo.toml
+++ b/Cargo.toml
@@ -40,14 +40,9 @@
 # block below) which serves both as documentation of which features
 # each example needs and as the gate for downstream library consumers
 # who attempt to build examples through their own build script.
-default = ["arch-qwen35", "arch-qwen35-vl", "arch-llama", "arch-qwen2", "arch-deepseek4", "arch-dots-ocr", "deltanet"]
+default = ["arch-qwen35", "deltanet"]
 deltanet = ["rdna-compute/deltanet", "hipfire-dispatch/deltanet"]
 arch-qwen35 = []
-arch-qwen35-vl = []
-arch-llama = []
-arch-qwen2 = []
-arch-deepseek4 = []
-arch-dots-ocr = []
 [dependencies]
 hip-bridge = { path = "../hip-bridge" }
 rdna-compute = { path = "../rdna-compute" }
@@ -96,7 +91,7 @@
 # features above and `required-features` on each `[[example]]` block
 # below.
 [dev-dependencies]
-hipfire-arch-qwen35 = { path = "../hipfire-arch-qwen35", optional = true }
+hipfire-arch-qwen35 = { path = "../hipfire-arch-qwen35" }
 hipfire-detect = { path = "../hipfire-detect" }
 hipfire-atlas = { path = "../hipfire-atlas" }
 
@@ -318,17 +313,6 @@
 name = "eval_hipfire"
 required-features = ["arch-qwen35", "deltanet"]
 
-# eval_hipfire_llama — KLD eval for llama-arch hipfire models (Qwen3 /
-# Qwen2 / Llama / Mistral / TinyLlama). Same HFKSEQ output as
-# eval_hipfire, but the forward path goes through
-# `<Llama as Architecture>` + `hipfire_runtime::llama::*` instead of
-# the qwen35 arch. Per-token scoring only — llama-arch
-# `forward_prefill_batch` doesn't expose the hidden-state capture hook
-# that eval_hipfire's prefill mode relies on.
-[[example]]
-name = "eval_hipfire_llama"
-required-features = ["arch-llama"]
-
 # Tokenizer-parity helper: reads a .hfq's tokenizer + tokenizes a text slice.
 # No GPU / arch features needed (tokenizer is pure CPU).
 [[example]]
