--- a/src/hipfire/mod.rs	2026-06-18 16:38:22.273524502 +0700
+++ b/src/hipfire/mod.rs	2026-06-18 16:38:22.275924465 +0700
@@ -28,7 +28,7 @@
         let home = env::var_os("HOME")
             .map(PathBuf::from)
             .unwrap_or_else(|| PathBuf::from("."));
-        let root = home.join(".hipfire");
+        let root = home.join(".hipfire-arwaky");
         let cwd = env::current_dir().unwrap_or_else(|_| PathBuf::from("."));
         Self {
             models: root.join("models"),

--- a/src/hipfire/status.rs	2026-06-18 16:38:22.273562022 +0700
+++ b/src/hipfire/status.rs	2026-06-18 16:38:22.277856298 +0700
@@ -33,7 +33,7 @@
         let (serve_http_ok, health_text) = probe_health(config);
         let gpu_lines = detect_gpu_lines();
         let paths_ok = vec![
-            ("~/.hipfire".into(), paths.root.exists()),
+            ("~/.hipfire-arwaky".into(), paths.root.exists()),
             ("models".into(), paths.models.exists()),
             ("config.json".into(), paths.config.exists()),
             (

--- a/src/ui.rs	2026-06-18 16:38:22.273637754 +0700
+++ b/src/ui.rs	2026-06-18 16:38:22.279164823 +0700
@@ -575,7 +575,7 @@
         )),
     ];
     if app.registry.local_files.is_empty() {
-        diagnostic_lines.push(Line::from("No local models under ~/.hipfire/models."));
+        diagnostic_lines.push(Line::from("No local models under ~/.hipfire-arwaky/models."));
     } else {
         diagnostic_lines.extend(
             app.registry
